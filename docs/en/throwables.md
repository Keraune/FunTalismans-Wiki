---
title: Throwables
nav_order: 9
---

# Throwables

Enable with:
```hocon
throwable { enabled: true }
```

Core fields:
- `cooldown`, `consumeItem`
- `projectile { type velocity gravity hitRadius maxLife }`
- `castSound` / `impactSound`
- `targets`, `particles`, `effects`
