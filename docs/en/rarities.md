---
title: Rarities
nav_order: 5
---

# Rarities

Loaded from `rarities/rarities.conf` under the `rarities { ... }` block.

Common fields:
- `id`, `displayName`, `color` (#RRGGBB), `weight`
- `modelData`, `lorePrefix`, `disableEnchantGlow`, `dropSettings` (if used)

